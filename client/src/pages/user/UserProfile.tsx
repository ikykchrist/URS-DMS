import AccountSecurity from "@/pages/AccountSecurity"

// User portal account page — delegates to the shared Account & Security
// surface (Sprint 8.1). The admin portal uses the same component.
export default function UserProfile() {
  return <AccountSecurity />
}
